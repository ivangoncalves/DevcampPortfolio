<p id="notice"><%= notice %></p>

<h1>Portfolio Items</h1>

<%= link_to "Create a new link", new_portfolio_path if logged_in?(:site_admin) %>

<%= render partial: "portfolio_item", collection: @portfolio_items %>
